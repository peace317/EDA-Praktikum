{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run this C Programm",
            "type": "process",
            "command": "make",
            "args": [
                "-g3",
                "${file}",
                "-o",
                "${fileDirname}\\${fileBasenameNoExtension}.exe"
            ],
            "isBackground": true,
            "detail": "compiler: make",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}